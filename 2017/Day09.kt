fun main(args: Array<String>) {
    val input = readLine()!!
    var skipNext = false
    var currentScore = 0
    var totalScore = 0
    var currentBlock = '0'
    for (character in input.toCharArray()) {
        if (!skipNext) {
            when (currentBlock) {
                '0' ->
                '{' ->
                '<' ->
            }
        }
    }
}